services:
  solr:
    image: solr:9.8.1
    container_name: solr_test
    ports:
      - "8983:8983"
    command: ["solr-precreate", "testcore"]

  solr-init:
    build:
      context: solr-init
    depends_on:
      - solr
    environment:
      - FORCE_REINDEX=false
    volumes:
      - ./solr-init/data/dataset.json:/opt/app/data/dataset.json
      - ../embeddings:/opt/app/embeddings
    working_dir: /opt/app
    command: ["python", "/opt/app/solr_init.py"]
